{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and gpu-mini windows",
  "windows": ["main", "gpu-mini"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
